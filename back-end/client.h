#pragma once

/**  \file client.h
	@brief Makes connection between user and back-end (Daemon).
	@brief Receives messages from user (input)
	@brief Sends to daemon a message which contains the requirement (writing a copy job, etc).
	


*/

/**	\fn main()
	@param None
	@returns 0
*/
int main();



